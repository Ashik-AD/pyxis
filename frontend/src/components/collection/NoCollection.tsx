import { FC } from "react";

const NoCollection: FC = () => {
  return (
    <div className="w-full h-full flex content-center">
      <div>Create your first collection</div>
    </div>
  );
};

export default NoCollection;
